# Production API Endpoint
# Automatically applied when building for live deployment (npm run build)
# Using a relative path (/api) ensures zero CORS errors and works on both HTTP and HTTPS!
VITE_API_URL=/api
